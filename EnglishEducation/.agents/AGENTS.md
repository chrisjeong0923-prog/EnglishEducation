# Custom Formatting Rules

- **Task Walkthrough Formatting**: When writing summaries or walkthrough reports of completed tasks (such as in `walkthrough.md`), always compile the process, files modified, and issues resolved for each task into separate tabs using the Markdown `carousel` syntax (separating each slide/tab with `<!-- slide -->`).
